🛡️ GRUB Sentinel

GRUB Sentinel — это мощный Bash-скрипт для автоматического восстановления, настройки и стилизации загрузчика GRUB2. Он решает проблемы с загрузкой, исправляет конфигурационные файлы, устанавливает поддержку HiDPI и позволяет интерактивно выбирать тему оформления и приоритет загрузки ОС.

✨ Возможности

🔧 Автоматическое восстановление:

Сброс и переустановка пакетов GRUB и shim-signed.

Восстановление стандартных скриптов в /etc/grub.d/ из репозитория.

Исправление блокировок dpkg и прерванных установок.

🎨 Кастомизация (Интерактивно):

Удобное меню выбора темы оформления (автоматический поиск тем в /boot/grub/themes и /usr/share/grub/themes).

Поддержка HiDPI мониторов (автоматическая установка высокого разрешения и gfxterm).

🚀 Управление загрузкой:

Интерактивный выбор операционной системы по умолчанию (парсит grub.cfg, очищает названия от мусора и прописывает полное имя в конфиг).

📦 Безопасность:

Автоматическое создание бэкапа конфигурации (/etc/default/grub, /etc/grub.d) перед внесением изменений.

Режим --dry-run для безопасной проверки действий без записи на диск.

🚀 Установка и запуск

1. Скачивание

Склонируйте репозиторий или скачайте скрипт напрямую:

git clone [https://github.com/zemcoviv/grub_sentinel.git](https://github.com/zemcoviv/grub_sentinel.git)
cd grub_sentinel


2. Права на выполнение

Сделайте скрипт исполняемым:

chmod +x grub_sentinel.sh


3. Запуск

Скрипт требует прав суперпользователя (root) для работы с системными файлами и EFI разделом:

sudo ./grub_sentinel.sh


⚙️ Аргументы командной строки

Вы можете использовать следующие флаги при запуске:

Аргумент

Описание

--dry-run

Режим симуляции. Показывает, какие команды будут выполнены, но не вносит реальных изменений в систему. Идеально для проверки.

--backup-dir DIR

Указать кастомную папку для сохранения резервных копий (по умолчанию: /var/backups/grub_sentinel).

--help

Показать справку по использованию.

Пример безопасного запуска (Dry Run):

sudo ./grub_sentinel.sh --dry-run


🛠 Как это работает

Логика скрипта построена на 4 последовательных этапах:

Backup & Fix: * Создается резервная копия конфигов.

Удаляются lock-файлы apt/dpkg и производится их настройка.

Prepare: * Скачиваются необходимые пакеты тем (grub2-themes-ubuntu-mate и др.).

Настраиваются параметры графики для HiDPI мониторов.

Interactive Selection:

Скрипт сканирует папки с темами и предлагает пользователю выбрать оформление.

Скрипт читает текущий grub.cfg, находит все записи menuentry, очищает их названия и предлагает выбрать ОС для автозагрузки.

Finalize:

Полная пересборка меню через update-grub.

Переустановка загрузчика в EFI раздел (grub-install).

Вывод итогового списка ОС, который увидит пользователь при перезагрузке.

⚠️ Дисклеймер

Этот инструмент вносит изменения в системные файлы загрузчика (/boot, /etc/default/grub, EFI раздел).
Несмотря на наличие функции бэкапа и тщательное тестирование на Ubuntu/Debian системах, автор не несет ответственности за возможные сбои.

Рекомендуется запускать с флагом --dry-run перед реальным применением.

📝 Лицензия

Распространяется под лицензией MIT. Подробности см. в файле LICENSE.
